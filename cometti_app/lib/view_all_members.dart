import 'package:flutter/material.dart';
class view_member extends StatelessWidget {
  const view_member({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
